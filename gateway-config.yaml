---
gateway:
  port: 8080
  timeout_seconds: 30

upstreams:
  user-service:
    targets:
      - host: "localhost"
        target_port: 8081
        protocol: http
      - host: "localhost"
        target_port: 8082
        protocol: http
      - host: "localhost"
        target_port: 8083
        protocol: http

  order-service:
    targets:
      - host: "localhost"
        target_port: 8082
        protocol: http

endpoints:
  - path: /users
    method: GET
    upstream: user-service
  - path: /orders
    method: GET
    upstream: order-service
